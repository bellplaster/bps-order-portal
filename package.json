{
  "name": "bps-order-portal-cloudflare-native",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name bps-order-portal",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/account-contacts-management.js && node --check public/account-role-ux.js && node --check functions/api/account-contacts.js",
    "test:xlsx": "node tools/generate-test-files.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
